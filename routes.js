// Global
const HOME = "/";
const JOIN = "/join";
const LOGIN = "/login";
const LOGOUT = "/logout";
const SEARCH = "/search";

// Users
const USERS = "/users";
const EDIT_PROFILE = "/edit-profile";

// Board
const PORTFOLIO = "/portfolio";
const PRACTICE_INTERVIEW = "/practice-interview";
const TEST_BOARD = "/test-board";

// Videos
const VIDEOS = "/videos";
const EDIT_VIDEOS = "/edit-videos";

// routes
const routes = {
  // Global
  home: HOME,
  join: JOIN,
  login: LOGIN,
  logout: LOGOUT,
  search: SEARCH,

  // Users
  users: USERS,
  editProfile: EDIT_PROFILE,

  // Board
  portfolio: PORTFOLIO,
  practiceInterview: PRACTICE_INTERVIEW,
  testBoard: TEST_BOARD,

  // Video
  videos: VIDEOS,
  editVideos: EDIT_VIDEOS
};

export default routes;
